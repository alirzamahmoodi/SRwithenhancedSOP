name: google-ai
channels:
  - defaults
  - pytorch
dependencies:
  - python = 3.9
  - cx_oracle
  - pip
  - pip:
      - google-generativeai
      - librosa
      - pydicom
      - pynetdicom
      - numpy
      - scipy
      - pyyaml
      - pytest
      - pytest-mock
      - pdfkit
      - watchdog